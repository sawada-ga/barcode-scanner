<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <meta name="theme-color" content="#667eea">
    <title>バーコードスキャナー</title>
    <link rel="stylesheet" href="style.css">
    <script src="https://unpkg.com/@zxing/library@latest"></script>
</head>
<body>
    <div class="container">
        <h1>📱 バーコードスキャナー</h1>
        
        <div class="camera-section">
            <div class="camera-frame">
                <video id="video" autoplay muted playsinline></video>
                <canvas id="canvas" style="display: none;"></canvas>
                <div class="scan-overlay">
                    <div class="scan-area"></div>
                    <div class="scan-line"></div>
                </div>
            </div>
            <p class="camera-hint">バーコードを枠内に合わせてください</p>
        </div>
        
        <div class="controls">
            <button id="startCamera" class="btn primary">📷 カメラ開始</button>
            <button id="stopCamera" class="btn secondary" disabled>⏹️ カメラ停止</button>
            <button id="capture" class="btn primary" disabled>📸 手動撮影</button>
            <button id="downloadCSV" class="btn success" disabled>📥 CSVダウンロード</button>
        </div>
        
        <div class="results">
            <h3>📋 読み取り結果 (<span id="codeCount">0</span>件)</h3>
            <div id="scannedCodes"></div>
        </div>
        
        <div class="manual-input">
            <h3>⌨️ 手動入力</h3>
            <div class="input-group">
                <input type="text" id="manualCode" placeholder="バーコードを手動で入力" inputmode="numeric">
                <button id="addManual" class="btn primary">追加</button>
            </div>
        </div>
        
        <div class="status-bar">
            <div id="status" class="status-text">カメラを開始してください</div>
        </div>
    </div>
    
    <script src="script.js"></script>
</body>
</html> 